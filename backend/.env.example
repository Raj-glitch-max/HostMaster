# Environment Configuration
NODE_ENV=development
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hostmaster_dev
DB_USER=postgres
DB_PASSWORD=your_password_here

# JWT Configuration
JWT_SECRET=your_jwt_secret_here_change_in_production
JWT_EXPIRE=7d

# Encryption Configuration (CRITICAL - for AWS credentials)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# MUST be 64 hex characters (32 bytes for AES-256)
ENCRYPTION_KEY=92cafb5a03df9cf5c0eab6601b0e6b079982a7ee84eb020547422ca43d569391

# AWS Configuration (use IAM roles in production, not credentials)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Logging
LOG_LEVEL=debug

# CORS
CORS_ORIGIN=http://localhost:3001
